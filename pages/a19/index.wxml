<view class="container">
  <items id='dialog' title='我是标题' content='恭喜你，学会了小程序组件' cancelText='知道了' confirm='谢谢你' bind:cancelEvent="_cancelEvent"
    bind:confirmEvent="_confirmEvent">
  </items>
  <button type="primary" bindtap="showDialog"> ClickMe! </button>
</view>


<test id="goodsList" wx:for="{{goodsArray}}" wx:key="unique" img="{{item.img}}" title="{{item.title}}"
  desc="{{item.desc}}" price="{{item.price}}" num="{{item.num}}">
</test>